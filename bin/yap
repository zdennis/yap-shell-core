#!/usr/bin/env ruby

file = __FILE__
if File.symlink?(file)
  file = File.readlink(file)
end

trap "SIGTSTP", "IGNORE"
trap "SIGINT", "IGNORE"
trap "SIGTTIN", "IGNORE"
trap "SIGTTOU", "IGNORE"

$LOAD_PATH.unshift File.dirname(file) + '/../lib'

require "yap"

STDOUT.sync = true
Yap.run_shell
